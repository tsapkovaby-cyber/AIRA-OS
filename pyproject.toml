[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aira-os"
version = "0.12.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["backend/agents/tests"]
addopts = "-q"
