# Asset Lineage

Every asset records request, provider/model, prompt version, references, scene controls, storage URI, checksum, rights/disclosure metadata, scores, and approvals. An edit creates a new asset with `parent_asset_id`; canonical files are never overwritten. AI generation/editing, human review, Founder approval, and platform disclosure remain independently auditable.
