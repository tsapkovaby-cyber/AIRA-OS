# Master Reference

`AIRA_MASTER_REFERENCE_V1` is registered as a read-only `asset://` reference. The binary remains in Asset Storage, not a database field or this repository. Its storage service owns the production checksum and backup replication. Replacement, mutation, deletion, or hierarchy changes require an explicit Founder workflow; ordinary registry operations deny overwrite.
