# Video Identity Lock

The lock combines immutable visual/voice identity versions with motion modes, maximum expression intensity, lip-sync threshold, minimum per-frame identity and maximum temporal drop. Sampling a single frame is prohibited. Quality cannot compensate for a wrong face or voice.
