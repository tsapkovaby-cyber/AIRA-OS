# Evaluation

Model profiles carry per-task evaluation scores used by quality-first routing. Future controlled model changes should compare schema success, revision and rejection rates, factual/task success, satisfaction, successful-task cost, and latency. Canary rollout and persistent evaluation datasets remain future work.

