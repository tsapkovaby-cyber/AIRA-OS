# Search Strategy

The query planner selects domains using intent terms and otherwise searches project, approved
memory, and public knowledge. Keyword search favors exact terminology while semantic search uses
the configured embedding provider. Why queries advertise bounded graph traversal in their plan.
Time constraints are applied using `valid_from` and `last_verified`; history APIs retain versions.

