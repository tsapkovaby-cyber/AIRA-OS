"use client";
import { FormEvent, useState } from "react";
export default function Login(){const [error,setError]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});if(r.ok)location.href="/dashboard";else setError("Credentials were not accepted. Reference: AUTH-401")}return <main className="login"><form className="login-card" onSubmit={submit}><p className="eyebrow">AIRA OS · SECURE ACCESS</p><h1>Founder Control Center</h1><p className="muted">Sign in to review and control supervised operations.</p><label className="field"><span>Email</span><input name="email" type="email" defaultValue="founder@aira.local" required autoFocus/></label><label className="field"><span>Password</span><input name="password" type="password" defaultValue="founder-demo" required/></label>{error&&<p role="alert" style={{color:"var(--red)"}}>{error}</p>}<button className="button primary" style={{width:"100%"}}>Sign in securely</button></form></main>}
