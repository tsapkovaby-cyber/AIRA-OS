"use client";import {useState} from "react";
export function ConfirmAction({label,danger=false}:{label:string,danger?:boolean}){const [open,setOpen]=useState(false),[done,setDone]=useState(false);async function confirm(){const r=await fetch('/api/actions',{method:'POST',headers:{'content-type':'application/json','x-csrf-token':'dashboard-demo'},body:JSON.stringify({action:label,objectVersion:1,reason:'Founder confirmed via dashboard'})});setOpen(false);setDone(r.ok)}return <>{<button className={`button ${danger?'danger':''}`} onClick={()=>setOpen(true)}>{label}</button>}{open&&<div className="dialog-backdrop" role="presentation"><section className="dialog" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title"><p className="eyebrow">Explicit confirmation required</p><h2 id="confirm-title">Confirm {label}?</h2><p>This action is submitted to the server, checked against your OWNER role, and written to the audit trail. No data will be deleted.</p><div className="dialog-actions"><button className="button" onClick={()=>setOpen(false)} autoFocus>Cancel</button><button className={`button ${danger?'danger':'primary'}`} onClick={confirm}>Confirm action</button></div></section></div>}{done&&<div role="status" className="toast">Action accepted and audited · AUD-3012</div>}</>}
