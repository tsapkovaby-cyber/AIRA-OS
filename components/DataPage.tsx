import {pages} from '@/lib/dashboard-data'; import {ConfirmAction} from './ConfirmAction';
export function DataPage({kind}:{kind:string}){const p=pages[kind];return <><div className="page-head"><div><p className="eyebrow">Founder Control Center</p><h1>{p.title}</h1><p className="muted">{p.subtitle}</p></div>{kind==='agents'?<div className="controls"><ConfirmAction label="Pause All Agents"/><ConfirmAction label="Resume Agents"/></div>:kind==='publishing'?<ConfirmAction label="Resume Publishing"/>:null}</div>{kind==='workflows'&&<div className="card" style={{marginBottom:16}}><b>WF-882 stage path</b><div className="workflow" style={{marginTop:12}}><span className="stage">✓ Research</span>→<span className="stage">✓ Knowledge</span>→<span className="stage">✓ Content</span>→<span className="stage">✓ Guardian</span>→<span className="stage wait">● Founder Approval</span>→<span className="stage">Publishing</span></div></div>}<div className="table-wrap"><table><thead><tr>{p.columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{p.rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j}>{j===0?<strong>{cell}</strong>:cell}</td>)}</tr>)}</tbody></table></div><p className="muted" style={{marginTop:12}}>Showing {p.rows.length} recent records · Detailed history is lazy-loaded and paginated.</p></>}
