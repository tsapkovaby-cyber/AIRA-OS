"use client"; import Link from "next/link"; import {usePathname} from "next/navigation";
const groups=[['CONTROL',['Overview','Approvals','AIRA Status']],['OPERATIONS',['Agents','Research','Knowledge','Memory','Content','Publishing','Workflows']],['GOVERNANCE',['Incidents','Costs','System Logs','Settings']]];
const slug=(n:string)=>n==='Overview'?'/dashboard':'/dashboard/'+n.toLowerCase().replaceAll(' ','-');
export function Shell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="shell"><aside className="sidebar"><div className="brand">AIRA / OS</div><nav className="nav" aria-label="Main navigation">{groups.map(([group,items])=><div key={group as string}><div className="nav-group">{group}</div>{(items as string[]).map(item=><Link className={path===slug(item)?'active':''} key={item} href={slug(item)}>{item}</Link>)}</div>)}</nav></aside><main className="main"><header className="topbar"><input className="search" aria-label="Search AIRA OS" placeholder="Search agents, workflows, knowledge…"/><div className="avatar"><b>FC</b><span><b>Founder</b><br/><small className="muted">OWNER</small></span></div></header>{children}</main></div>}
