# Identity Engine Interfaces

- `IdentityInterface` defines the subsystem boundary.
- `IdentityConfig` carries environment-derived configuration.
- `IdentityObject` is the architecture-level output object.
